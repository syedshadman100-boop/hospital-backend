{
  "name": "@hms/prisma",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:prod": "prisma migrate deploy",
    "db-push": "prisma db push",
    "seed": "ts-node --compiler-options {\"module\":\"commonjs\"} ./prisma/seed.ts",
    "seed:doctor": "node --require ts-node/register ./prisma/seed-doctor.ts",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/adapter-mariadb": "^7.9.0",
    "@prisma/client": "^7.8.0",
    "bcryptjs": "^3.0.3",
    "mariadb": "^3.5.3"
  },
  "devDependencies": {
    "prisma": "^7.8.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0"
  }
}
